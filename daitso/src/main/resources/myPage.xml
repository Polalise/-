<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="myPagens">
<!-- 내 정보 전부 받아오기 -->
<select id="myInfo">
	select * from member where id=#{id}
</select>
<!-- 내 정보 수정하기 -->
<update id="updateProfile">
	update member set nickName=#{nickName},address=#{address},
	phone=#{phone},email=#{email},photoName=#{photoName}
	where id=#{id}
</update>
<!-- 내 정보중 레벨 받아오기 -->
<select id="myLevel">
	select grade from member where id=#{id}
</select>
<!-- 내가 산 물품 목록 가져오기 -->
<select id="myBuyList">
	select * from product where p_write=#{id} and p_value='구매'
</select>
<!-- 내가 판 물품 목록 가져오기 -->
<select id="mySellList">
	select * from product where p_write=#{id} and p_value='판매'
</select>
<!-- 내 좋아요 물품 목록 가져오기 -->
<select id="likeList">
	select * from likes where id=#{id}
</select>

</mapper>